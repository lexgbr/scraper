{
  "name": "@svp/db",
  "private": true,
  "type": "module",
  "dependencies": {
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  },
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate dev --name init"
  }
}
